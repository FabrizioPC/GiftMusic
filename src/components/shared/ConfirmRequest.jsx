import React from "react";

const ConfirmRequest = ({ messageTitle, confirmText = "Si" }) => {
   return (
      <article>
         <h2>{messageTitle}</h2>
         <button></button>
      </article>
   );
};

export default ConfirmRequest;
